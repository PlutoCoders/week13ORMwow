const { Product, ProductTag, Tag, Category } = require("./models");

// We will be retrieving data from our sql database using Sequelize and sending them back as a JSON response.

// Get Tags

// Get Categories

// Get Categories (by id)

// Get Products

// Get Product Tags

// Export all the function queries